% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ds.exploreHyperParameter.R
\name{ds.semiOPBARTExploreHyperE}
\alias{ds.semiOPBARTExploreHyperE}
\title{Client: Explore hyperparameters for Architecture E, using the real
ds.semiOPBARTTrainE() / ds.semiOPBARTPredictE() / ds.semiOPBARTEvaluate()
pipeline (periodic theta/us sync, trees always local) for every grid point.}
\usage{
ds.semiOPBARTExploreHyperE(
  conns,
  data.name,
  outcome_col,
  levels,
  x_features,
  w_features,
  test_data.name = NULL,
  n_samp_range = c(500, 1000, 2000),
  n_burn_range = c(500, 1000, 2000),
  n_tree_range = c(50, 100, 200),
  seed_range = c(42, 123, 456),
  k_values = c(1, 2, 3),
  warmup_burn = NULL,
  max_configs = NULL,
  nfilter = 5,
  verbose = TRUE,
  state_name = ".semiOPBART_hyper_E"
)
}
\arguments{
\item{conns}{list of DataSHIELD connections to the sites}

\item{data.name}{the TRAIN object at each site}

\item{outcome_col, x_features, w_features}{unused by Architecture E's own
training call (ds.semiOPBARTTrainE() reads directly from the already-
prepared `data.name` object) but kept for a consistent signature across
ds.semiOPBARTExploreHyperF/E/D() and for evaluate()'s `levels`}

\item{levels}{factor levels of the outcome (forwarded to ds.semiOPBARTEvaluate())}

\item{test_data.name}{name of the TEST object at each site (optional)}

\item{n_samp_range, n_burn_range, n_tree_range, seed_range, k_values}{grids to sweep}

\item{warmup_burn}{warm-start iterations before the main loop, forwarded to
ds.semiOPBARTTrainE(); default `min(50, n_burn)` per config, kept short
because hyperparameter exploration is already many configs x many sweeps}

\item{max_configs}{cap the grid at this many randomly-sampled rows (optional)}

\item{nfilter}{disclosure floor forwarded to train/predict/evaluate}

\item{verbose}{print progress}

\item{state_name}{base name for this run's per-config server-side state}
}
\description{
Client: Explore hyperparameters for Architecture E, using the real
ds.semiOPBARTTrainE() / ds.semiOPBARTPredictE() / ds.semiOPBARTEvaluate()
pipeline (periodic theta/us sync, trees always local) for every grid point.
}
