% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ds.exploreHyperParameter.R
\name{ds.semiOPBARTExploreHyperD}
\alias{ds.semiOPBARTExploreHyperD}
\title{Client: Explore hyperparameters for Architecture D, using the real
ds.semiOPBARTTrain() / ds.semiOPBARTPredict() / ds.semiOPBARTEvaluate()
pipeline (periodic theta/us sync + tree swapping) for every grid point.}
\usage{
ds.semiOPBARTExploreHyperD(
  conns,
  data.name,
  outcome_col,
  levels,
  x_features,
  w_features,
  test_data.name = NULL,
  n_samp_range = c(500, 1000, 2000),
  n_burn_range = c(500, 1000, 2000),
  n_tree_range = c(50, 100, 200),
  seed_range = c(42, 123, 456),
  k_values = c(1, 2, 3),
  n_swap_range = c(2, 4, 8),
  warmup_burn = NULL,
  max_configs = NULL,
  nfilter = 5,
  verbose = TRUE,
  state_name = ".semiOPBART_hyper_D"
)
}
\arguments{
\item{conns}{list of DataSHIELD connections to the sites}

\item{data.name}{the TRAIN object at each site}

\item{outcome_col, x_features, w_features}{kept for signature consistency
with ds.semiOPBARTExploreHyperF/E(); not used directly by Architecture D's
own training call}

\item{levels}{factor levels of the outcome (forwarded to ds.semiOPBARTEvaluate())}

\item{test_data.name}{name of the TEST object at each site (optional)}

\item{n_samp_range, n_burn_range, n_tree_range, seed_range, k_values}{grids to sweep}

\item{n_swap_range}{number of trees swapped per rotation; grid is filtered
to n_swap <= n_tree (a config can't swap more trees than it owns)}

\item{warmup_burn}{warm-start iterations before the main loop, forwarded to
ds.semiOPBARTTrain(); default `min(50, n_burn)` per config}

\item{max_configs}{cap the grid at this many randomly-sampled rows (optional)}

\item{nfilter}{disclosure floor forwarded to train/predict/evaluate}

\item{verbose}{print progress}

\item{state_name}{base name for this run's per-config server-side state}
}
\description{
Client: Explore hyperparameters for Architecture D, using the real
ds.semiOPBARTTrain() / ds.semiOPBARTPredict() / ds.semiOPBARTEvaluate()
pipeline (periodic theta/us sync + tree swapping) for every grid point.
}
