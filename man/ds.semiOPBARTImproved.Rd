% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ds.semiopbart.R
\name{ds.semiOPBARTImproved}
\alias{ds.semiOPBARTImproved}
\title{ds.semiOPBARTImproved - Architecture D with optional adaptive communication}
\usage{
ds.semiOPBARTImproved(
  data.name = "semiOPBART_train",
  datasources = NULL,
  num_tree = 20,
  k = 1,
  num_burn = 1000,
  num_save = 1000,
  theta_every = NULL,
  us_every = NULL,
  sync_every = NULL,
  initial_every = 50,
  min_every = 5,
  max_every = 200,
  warmup_burn = 100,
  adapt_rate = 0.1,
  n_swap = 4,
  nfilter_threshold = 5,
  threshold_method = c("percentile", "exact"),
  state_name = ".semiOPBART_state",
  allow_local_ecdf = TRUE,
  seed = 35
)
}
\arguments{
\item{data.name}{TRAIN object name}

\item{datasources}{list of DataSHIELD connections}

\item{num_tree}{number of trees per site}

\item{k}{BART shrinkage parameter}

\item{num_burn}{number of burn-in iterations}

\item{num_save}{number of saving iterations}

\item{theta_every}{frequency for theta aggregation. If NULL (default), 
uses adaptive communication. If numeric, uses fixed 
periodic communication.}

\item{us_every}{frequency for threshold aggregation. If NULL (default), 
uses adaptive communication. If numeric, uses fixed 
periodic communication.}

\item{sync_every}{frequency for tree swapping. If NULL (default), 
uses adaptive communication. If numeric, uses fixed 
periodic communication.}

\item{initial_every}{initial communication frequency (only used when 
adaptive is enabled for a parameter)}

\item{min_every}{minimum communication interval (adaptive only)}

\item{max_every}{maximum communication interval (adaptive only)}

\item{warmup_burn}{number of warmup iterations with aggressive communication}

\item{adapt_rate}{local adaptation rate during long gaps (0-1)}

\item{n_swap}{number of trees to swap per event}

\item{threshold_method}{"percentile" or "exact"}

\item{allow_local_ecdf}{allow local_ecdf with tree sharing}
}
\description{
ds.semiOPBARTImproved - Architecture D with optional adaptive communication
}
