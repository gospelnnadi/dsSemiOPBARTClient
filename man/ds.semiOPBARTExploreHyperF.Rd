% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ds.exploreHyperParameter.R
\name{ds.semiOPBARTExploreHyperF}
\alias{ds.semiOPBARTExploreHyperF}
\title{Client: Explore hyperparameters for Architecture F, using the real
ds.semiOPBARTFitF() / ds.semiOPBARTCombineF() / ds.semiOPBARTPredictF()
/ ds.semiOPBARTEvaluate() pipeline for every grid point.}
\usage{
ds.semiOPBARTExploreHyperF(
  conns,
  data.name,
  outcome_col,
  levels,
  x_features,
  w_features,
  test_data.name = NULL,
  n_samp_range = c(500, 1000, 2000),
  n_burn_range = c(500, 1000, 2000),
  n_tree_range = c(50, 100, 200),
  seed_range = c(42, 123, 456),
  k_values = c(1, 2, 3),
  max_configs = NULL,
  nfilter = 5,
  combine_method = c("random_effects", "inverse_variance", "stack"),
  verbose = TRUE,
  state_name = ".semiOPBART_hyper",
  sd = 1
)
}
\arguments{
\item{conns}{list of DataSHIELD connections to the sites}

\item{data.name}{the TRAIN object at each site (from ds.semiOPBARTPrepare()/Split())}

\item{outcome_col}{name of the outcome column in the data}

\item{levels}{factor levels of the outcome variable (analyst-specified,
same set passed to ds.semiOPBARTPrepare())}

\item{x_features}{vector of names of the predictor columns}

\item{w_features}{vector of names of the linear predictor columns}

\item{test_data.name}{name of the TEST object at each site (optional;
without it, only n_sites/config metadata is reported, no test metrics)}

\item{n_samp_range, n_burn_range, n_tree_range, seed_range, k_values}{grids
to sweep, forwarded to ds.semiOPBARTFitF()'s num_save/num_burn/num_tree/seed/k}

\item{max_configs}{cap the grid at this many randomly-sampled rows (optional)}

\item{nfilter}{disclosure floor forwarded to fit/predict/evaluate}

\item{combine_method}{"inverse_variance" or "stack" -- forwarded to ds.semiOPBARTCombineF()}

\item{verbose}{print progress}

\item{state_name}{base name for this run's per-config server-side state}
}
\description{
Client: Explore hyperparameters for Architecture F, using the real
ds.semiOPBARTFitF() / ds.semiOPBARTCombineF() / ds.semiOPBARTPredictF()
/ ds.semiOPBARTEvaluate() pipeline for every grid point.
}
