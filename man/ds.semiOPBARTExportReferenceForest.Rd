% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ds.externalPredict.R
\name{ds.semiOPBARTExportReferenceForest}
\alias{ds.semiOPBARTExportReferenceForest}
\title{Pull ONE already-trained site's own current forest, to be shipped
elsewhere as the reference model for never-trained-site prediction.
Deliberately restricted to a single site -- this is what makes it a
"reference site" mechanism, not a second pooling step.}
\usage{
ds.semiOPBARTExportReferenceForest(fit, reference_site, datasources = NULL)
}
\arguments{
\item{fit}{output of ds.semiOPBARTTrain() -- used only to
confirm `reference_site` actually trained (fit$site_names)}

\item{reference_site}{a single site name, must be one of `fit$site_names`}
}
\description{
Pull ONE already-trained site's own current forest, to be shipped
elsewhere as the reference model for never-trained-site prediction.
Deliberately restricted to a single site -- this is what makes it a
"reference site" mechanism, not a second pooling step.
}
