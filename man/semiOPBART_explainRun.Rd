% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ds.SemiOPBARTExplain.R
\name{semiOPBART_explainRun}
\alias{semiOPBART_explainRun}
\title{Build (and optionally save) coefficient, threshold, and variable-
importance summaries/plots for every architecture (D/E/F) present in
one run_semiOPBART_orchestrator() result. No orchestrator changes
needed -- this consumes its return value.}
\usage{
semiOPBART_explainRun(
  run_result,
  w_features = NULL,
  datasources = NULL,
  out_dir = NULL,
  weight_by_n = TRUE
)
}
\arguments{
\item{run_result}{one run_semiOPBART_orchestrator() result (has fit_D/
fit_E/fit_F for whichever architectures were run; others are NULL and
silently skipped)}

\item{w_features}{the SAME w_features passed to that orchestrator call}

\item{datasources}{needed only if fit_D/fit_E is present (variable
importance needs one fresh aggregate() round trip); NULL default
finds every current connection}

\item{out_dir}{if given, saves one CSV per summary and one PNG per
plot here (created if missing); NULL (default) returns everything
without writing to disk}

\item{weight_by_n}{passed to ds.semiOPBARTVarImportance() for D/E}
}
\description{
Build (and optionally save) coefficient, threshold, and variable-
importance summaries/plots for every architecture (D/E/F) present in
one run_semiOPBART_orchestrator() result. No orchestrator changes
needed -- this consumes its return value.
}
