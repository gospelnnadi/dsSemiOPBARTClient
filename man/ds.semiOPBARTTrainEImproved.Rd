% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ds.semiopbart.R
\name{ds.semiOPBARTTrainEImproved}
\alias{ds.semiOPBARTTrainEImproved}
\title{ds.semiOPBARTTrainEImproved - Architecture E with optional adaptive communication}
\usage{
ds.semiOPBARTTrainEImproved(
  data.name = "D_train",
  datasources = NULL,
  num_tree = 20,
  k = 1,
  num_burn = 1000,
  num_save = 1000,
  sync_every = NULL,
  initial_every = 50,
  min_every = 5,
  max_every = 200,
  warmup_burn = 100,
  adapt_rate = 0.1,
  nfilter_threshold = 5,
  threshold_method = c("percentile", "exact"),
  state_name = ".semiOPBART_state",
  seed = 35
)
}
\arguments{
\item{data.name}{TRAIN object name}

\item{datasources}{list of DataSHIELD connections}

\item{num_tree}{number of trees per site}

\item{k}{BART shrinkage parameter}

\item{num_burn}{number of burn-in iterations}

\item{num_save}{number of saving iterations}

\item{sync_every}{frequency for theta/us aggregation. If NULL (default), 
uses adaptive communication. If numeric, uses fixed 
periodic communication.}

\item{initial_every}{initial communication frequency (adaptive only)}

\item{min_every}{minimum communication interval (adaptive only)}

\item{max_every}{maximum communication interval (adaptive only)}

\item{warmup_burn}{number of warmup iterations}

\item{adapt_rate}{local adaptation rate during long gaps}

\item{threshold_method}{"percentile" or "exact"}

\item{state_name}{where to find/save local state}
}
\description{
Architecture E: Only theta/us are synchronized periodically.
Trees remain entirely local at each site.
}
